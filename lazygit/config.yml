gui:
  showIcons: true
  nerdFontsVersion: "3"
  border: single
  theme:
    activeBorderColor:
      - blue
      - bold
git:
  overrideGpg: true

os:
  edit: '[ -z "$NVIM" ] && (nvim -- {{filename}}) || (nvim --server "$NVIM" --remote-send "q" && nvim --server "$NVIM" --remote {{filename}})'
